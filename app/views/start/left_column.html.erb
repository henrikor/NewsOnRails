    <% for group in @groups_left_column %>
      <%= link_to group.name, :controller => "start", :action => "group", :id => group.group_id %> <br/>
    <% end %>

    <br/>
    <br/>
    <% for article in @articles_left_column %>
      <%= link_to article.headline, :controller => "start", :action =>
          "view", :id => article.article_id %> <br/>
    <% end %>

